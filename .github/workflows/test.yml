name: workflow_dispatch test remove inputs
on:
  workflow_dispatch:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - run: 
        echo $GITHUB_SHA
        echo $GITHUB_REF
  